import { View } from "react-native";

type ToggleProps = {
    function: () => void;
}

export default function ToggleBase(props: ToggleProps) {
    return (
        <View>
            
        </View>
    )
}