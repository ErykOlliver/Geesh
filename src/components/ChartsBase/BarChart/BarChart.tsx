import React from "react";
import { View, Text } from "react-native";
import Svg, { Rect } from "react-native-safe-area-context";

type BarChartProps = {
    data: string[]
}

export default function BarChart(props: BarChartProps) {
    return (
        <View>
        </View>
    )
}